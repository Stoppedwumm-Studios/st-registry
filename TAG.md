<!---
Replace PACKAGE_NAME with your package name
-->
**Install this package via spm**
```bash
npm i -g st-registry-cli
spm install PACKAGE_NAME
# or use execute (exec) to directly run it (if supported)
spm exec PACKAGE_NAME
```